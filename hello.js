function hello (name) {
   let pharse = 'hello, ${name} !';
    say (phase);

}

/*function say (phrase) {
    alert ( '** ${pharse} **');
}


function toggleClock()   {
    let myClock= document.getElementById('clock');
    let settings = myClock.style.display;
    let clockButton = document.getElementById('clockButton')

    if(settings == 'block')  {
        myClock.style.display= 'none';
        clockButton.innerHTML = 'Show';

    } else {
        myClock.style.display = 'block';
        clockButton.innerHTML= 'Hide';
    }
}
*/

























